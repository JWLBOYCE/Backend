PORT=8080
# Comma-separated origins allowed for CORS
ALLOWED_ORIGINS=https://dashboard.accessairbility.com

# SQLite DB path on the host (mounted read-only in production)
DATABASE_PATH=/opt/aab/data/main.db

# Optional bearer token to protect API routes
# If set, clients must send Authorization: Bearer <API_TOKEN>
API_TOKEN=

# Rate limiting
RATE_LIMIT_MAX=300
RATE_LIMIT_WINDOW=1 minute
RATE_LIMIT_ALLOWLIST=
